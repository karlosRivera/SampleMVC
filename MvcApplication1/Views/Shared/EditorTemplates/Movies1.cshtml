@model List<MvcApplication1.Models.Movies>

@for (int i = 0; i < Model.Count; i++)
{
<tr>
    <td>
        @Html.HiddenFor(x => Model[i].ID)
        @Html.CheckBoxFor(x => Model[i].IsSelected)
        @Html.DisplayFor(x => Model[i].Name)
        @Html.HiddenFor(x => Model[i].Name)
    </td>
</tr>
}